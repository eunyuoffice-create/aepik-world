# 애픽 월드 (Aepik World) — 유저 플로우 (User Flow)

> **버전**: v1.3
> **기반 문서**: PRD v5.0
> **작성일**: 2026-03-06
> **용도**: HTML 와이어프레임 구축 시 참고 문서

---

## 1. 전체 서비스 플로우 개요

```
┌─────────────────────────────────────────────────────────────┐
│                     애픽 월드 서비스 진입                          │
│                                                             │
│  [비로그인 유저]              [로그인 유저]                    │
│       │                          │                          │
│       ▼                          ▼                          │
│  매물 검색/열람             모든 기능 이용                     │
│  이벤트 캘린더 열람          교환글 등록                       │
│       │                    공구 참여                         │
│       │                    채팅                              │
│       ▼                    이벤트 등록                        │
│  [가입 유도 팝업]           배너 신청                          │
│       │                          │                          │
│       ▼                          │                          │
│  소셜 로그인                      │                          │
│       │                          │                          │
│       ▼                          │                          │
│  [첫 교환/공구 시도]              │                          │
│       │                          │                          │
│       ▼                          ▼                          │
│  본인인증 요청 ──────────→ 전체 기능 이용                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 회원가입 & 인증 플로우

### 2-0. 로그인 [AUTH01]

```
[앱/웹 진입]
    │
    ▼
[AUTH01-A 로그인 메인]
    │
    ├── [카카오 로그인] → 카카오 OAuth 팝업/리다이렉트
    │       ├── 기존 회원 → JWT 발급 → HOME01
    │       └── 신규 회원 → AUTH02 (회원가입)
    │
    ├── [이메일 로그인] → AUTH01-B 이메일 로그인 모드
    │       ├── 이메일 + 비밀번호 입력
    │       ├── [로그인 버튼] → API 호출
    │       │     ├── 성공 → JWT 발급 → HOME01
    │       │     ├── 실패 → 인라인 에러 "이메일 또는 비밀번호가 일치하지 않습니다"
    │       │     └── 5회 초과 실패 → 30분 계정 잠금
    │       ├── [비밀번호를 잊으셨나요?] → AUTH01-C 비밀번호 찾기
    │       │     ├── 이메일 입력 → 재설정 링크 발송 (유효 1시간)
    │       │     └── 성공 → "메일이 전송되었습니다" 안내
    │       └── [회원가입] → AUTH02
    │
    ├── [로그인 없이 둘러보기] → 비로그인 세션 → HOME01 (검색/열람만 가능)
    │
    └── [약관 안내] → 이용약관/개인정보처리방침 링크 → STAT01
```

**비로그인 상태 기능 제한 시 → 로그인 유도 바텀시트 [AUTH01-D]**
```
[교환글 등록/공구 참여/채팅/찜/신고 시도]
    │
    ▼
[AUTH01-D 로그인 유도 바텀시트]
    ├── "로그인이 필요한 기능입니다" 안내
    ├── [카카오 로그인] → OAuth 진행
    ├── [이메일 로그인] → AUTH01-B
    └── [나중에] → 바텀시트 닫기, 현재 화면 유지
```

**특수 계정 상태 처리**
```
[로그인 시도]
    ├── 휴면 계정 (1년 미접속) → 휴면 해제 안내 팝업 → 본인인증 후 복구
    ├── 정지 계정 → 정지 안내 + 사유 + 해제일 표시, 기능 차단
    └── OAuth 에러 → 에러 페이지 + "다시 시도" / "이메일로 로그인" 안내
```

### 2-1. 회원가입 [AUTH02]

```
[AUTH01에서 진입]
    ├── 카카오 신규 → Step 1 → (Step 2 건너뜀) → Step 3 → Step 4
    └── "회원가입" 링크 → Step 1 → Step 2 → Step 3 → Step 4
        │
        ▼
[AUTH02-A Step 1: 약관 동의]
    ├── [전체 동의] → 모든 체크박스 일괄 선택/해제
    ├── [필수] 이용약관 동의 + "보기" (STAT01)
    ├── [필수] 개인정보 수집 및 이용 동의 + "보기"
    ├── [선택] 마케팅 정보 수신 동의
    ├── [필수] 만 14세 이상 확인
    └── [다음] → 필수 항목 모두 체크 시 활성화
        │
        ▼
[AUTH02-B Step 2: 계정 정보 입력] (이메일 가입만, 카카오 경유 시 건너뜀)
    ├── 이메일 입력 → 형식 검증 + 자동 중복 확인
    ├── 비밀번호 입력 (8자+, 영문+숫자+특수문자) → 강도 바 (약함/보통/강함)
    ├── 비밀번호 확인 → 일치 여부 실시간 표시
    └── [다음] → 모든 유효성 통과 시 활성화
        │
        ▼
[AUTH02-C Step 3: 프로필 설정]
    ├── 프로필 이미지 (선택, 80px 원형, 미등록 시 기본 아바타)
    ├── 닉네임 입력 (2~12자, 한글/영문/숫자)
    │     ├── debounce 500ms 후 중복 확인
    │     └── 금칙어 필터 (비속어, 관리자 사칭 차단)
    └── [다음] → 닉네임 유효 + 중복확인 통과 시 활성화
        │
        ▼
[AUTH02-D Step 4: 관심 카테고리 선택] (선택사항)
    ├── 애니/만화/소설 (Purple) | 아이돌/아티스트 (Pink)
    ├── 배우/무대 (Amber) | 게임 (Cyan)
    ├── 복수 선택 가능, 미선택도 허용
    ├── [시작하기] → 회원가입 완료
    └── [나중에 설정할게요] → 카테고리 미선택 상태로 완료
        │
        ▼
[AUTH02-E 가입 완료]
    ├── "{닉네임}님, 환영합니다!" 메시지
    ├── [시작하기] → JWT 발급 → HOME01
    └── (24시간 내 웰컴 푸시 알림)
```

### 2-2. 본인인증 (Just-in-time)

```
[교환글 등록 또는 공구 참여 첫 시도]
    │
    ▼
[본인인증 필요 안내 바텀시트]
    │
    ├── [인증하기] → PortOne 본인인증 팝업
    │                    │
    │                    ├── 성공 → [인증 완료 뱃지 부여] → 원래 작업 계속
    │                    └── 실패 → 재시도 안내
    │
    └── [나중에] → 원래 화면으로 복귀 (기능 제한 유지)
```

---

## 3. 메인 홈 플로우

```
[홈 화면]
    │
    ├── [검색바 탭] ──────────────────→ 3-1. 검색 플로우
    │
    ├── [배너 캐러셀 탭] ─────────────→ 배너 상세 (배너 주인공 관련 매물 목록)
    │
    ├── [다가오는 이벤트 섹션]
    │       ├── [이벤트 카드 탭] ────→ 이벤트 상세 페이지
    │       └── [더보기] ────────────→ 이벤트 캘린더 페이지
    │
    ├── [인기 교환 섹션]
    │       ├── [상품 카드 탭] ──────→ 교환 상세 페이지
    │       └── [더보기] ────────────→ 교환 목록 페이지
    │
    ├── [진행 중 공구 섹션]
    │       ├── [공구 카드 탭] ──────→ 공구 상세 페이지
    │       └── [더보기] ────────────→ 공구 목록 페이지
    │
    ├── [구함글 섹션]
    │       ├── [구함글 탭] ─────────→ 구함글 상세
    │       └── [더보기] ────────────→ 구함글 목록 페이지
    │
    ├── [`Bell` 알림 아이콘] ────────────→ 알림 목록 페이지
    │
    └── [☰ 메뉴 아이콘] ────────────→ 사이드 메뉴
```

### 3-1. 검색 플로우

```
[검색바 탭] (홈 상단 검색바 / 하단 네비 돋보기)
    │
    ▼
[검색 입력 모드 — SRCH01-A]
    │
    ├── [인기 검색어] → 탭 시 즉시 검색 실행
    │
    ├── [최근 검색어] → 탭 시 재검색 / [X] 개별 삭제 / [전체 삭제]
    │
    └── [텍스트 입력]
            │
            ├── 2글자 이상 → 실시간 자동완성 (작품명 > 캐릭터명 우선, 300ms 디바운스)
            │     └── 자동완성 항목 탭 → 즉시 검색 실행
            │
            └── [검색 버튼 / 엔터]
                    │
                    ▼
                [검색 결과 모드 — SRCH01-B]
                    │
                    ├── [탭 바] 전체 / 교환 / 공구 / 구함 / 이벤트
                    │
                    ├── [필터 칩 바] 작품 ▾ | 캐릭터 ▾ | 필터 (아이콘)
                    │     │
                    │     └── [필터 아이콘 탭]
                    │             │
                    │             ▼
                    │         [필터 바텀시트 — SRCH01-C]
                    │             ├── 카테고리 (4대 칩)
                    │             ├── 굿즈 타입 (다중 선택)
                    │             ├── 상품 상태 (S/A/B/C급)
                    │             ├── 거래 방법 (택배/직거래/둘 다)
                    │             ├── 정렬 (최신/인기/가까운)
                    │             ├── 추가 옵션 (차액 협의 가능만)
                    │             │
                    │             ├── [초기화] → 모든 필터 리셋
                    │             └── [N건 보기] → 필터 적용 + 바텀시트 닫기
                    │
                    ├── [정렬] (최신순 / 인기순 / 가까운순)
                    │
                    ├── [결과 카드 탭] → 해당 상세 페이지 (EXCH02/GRPB02/WANT01/EVNT02)
                    │
                    └── [검색 결과 없음] → 빈 상태 + 추천 검색어 제안
```

---

## 4. 교환 플로우

### 4-1. 교환글 등록

```
[FAB(+) 버튼 또는 등록하기 버튼]
    │
    ▼
[거래 유형 선택] ── 교환 / 공구 / 구함
    │
    │ (교환 선택)
    ▼
[교환글 등록 폼 — Step 1: 보유 굿즈]
    ├── 사진 업로드 (최대 5장)
    ├── 작품 검색/선택
    ├── 캐릭터 선택
    ├── 굿즈 타입 선택
    ├── 상품 상태 (S/A/B/C급)
    ├── 보유 수량
    └── [다음]
        │
        ▼
[교환글 등록 폼 — Step 2: 원하는 굿즈]
    ├── 원하는 작품/캐릭터/굿즈타입
    ├── 원하는 수량
    ├── 차액 협의 가능 여부 (체크박스)
    │     └── (체크 시) 희망 차액 금액 입력
    └── [다음]
        │
        ▼
[교환글 등록 폼 — Step 3: 거래 방식]
    ├── 택배 / 직거래 / 둘 다
    │     └── (직거래 선택 시) 지도에서 위치 선택 + 설명 입력
    ├── 추가 조건 (텍스트, 선택)
    └── [등록하기]
        │
        ▼
[본인인증 체크] ── 미인증 시 → 2-2. 본인인증 플로우
    │
    ▼
[등록 완료]
    ├── 교환글 상세 페이지로 이동
    └── (백그라운드) 매칭 알고리즘 실행 → 매칭 시 양쪽 푸시 알림
```

### 4-2. 교환 매칭 & 진행

```
[매칭 알림 수신] (FCM 푸시)
    │
    ▼
[알림 탭] → 매칭된 교환글 상세 확인
    │
    ▼
[교환 상세 페이지]
    │
    ├── [`MessageCircle` 채팅] → 채팅방 개설/입장
    │       │
    │       ▼
    │   [채팅에서 조건 협의]
    │       ├── 교환 수량 확인
    │       ├── 차액 금액 협의 (있는 경우)
    │       ├── 거래 방식 확인 (택배/직거래)
    │       └── 양쪽 합의
    │           │
    │           ▼
    │       [교환 확정 버튼 클릭] (양쪽 모두)
    │           │
    │           ▼
    │       ── 택배 거래 ──              ── 직거래 ──
    │           │                           │
    │           ▼                           ▼
    │       [차액 정산]               [일정/장소 확정]
    │       (직접 송금)                     │
    │           │                           ▼
    │           ▼                       [만남 & 교환]
    │       [각자 발송]                     │
    │       (운송장 번호 입력)               ▼
    │           │                       [교환 완료 버튼]
    │           ▼                       (양쪽 모두)
    │       [수취 확인]                     │
    │       (양쪽 모두)                     │
    │           │                           │
    │           └──────────┬────────────────┘
    │                      │
    │                      ▼
    │              [교환 완료 처리]
    │                      │
    │                      ▼
    │              [후기 작성 요청 알림]
    │                      │
    │                      ▼
    │              [후기 작성] (7일 이내)
    │                      │
    │                      ├── 긍정 / 부정
    │                      └── 텍스트 후기 (선택)
    │
    └── [`Repeat` 교환 제안] → 내 보유 굿즈 선택 → 교환 제안 전송
            │
            └── 상대방에게 교환 제안 알림 → 채팅방 자동 생성
```

### 4-3. 한쪽만 완료 확인 시

```
[A가 교환 완료 클릭]
    │
    ▼
[B에게 완료 확인 요청 알림]
    │
    ├── B가 5일 내 확인 → 정상 완료
    └── B가 5일간 미응답 → 자동 완료 처리
```

### 4-4. 교환 제안 관리 [EXCH05]

```
[교환 신청함 진입] (마이페이지 > 교환 신청함 / 알림 탭)
    │
    ▼
[EXCH05 교환 신청함 메인]
    │
    ├── [보낸 제안 탭]
    │     │
    │     ├── 제안 카드 목록 (상태별 필터: 전체/대기중/수락/거절/만료)
    │     │     ├── 대기중(pending) → [취소] 가능
    │     │     ├── 수락됨(accepted) → 채팅방 이동
    │     │     ├── 거절됨(rejected) → 거절 사유 확인
    │     │     ├── 취소됨(cancelled) → 상태 표시만
    │     │     └── 만료(expired) → "7일 미응답 자동 만료"
    │     │
    │     └── [제안 카드 탭] → 해당 교환글 상세(EXCH02)
    │
    └── [받은 제안 탭]
          │
          ├── 교환글별 그룹핑 (내 교환글 > 받은 제안 리스트)
          │     ├── 제안자 정보 (닉네임, 신뢰등급, 프로필)
          │     ├── 제안 굿즈 정보 (이미지, 상태, 조건)
          │     └── 제안 메시지 (있는 경우)
          │
          ├── [수락] 버튼
          │     │
          │     ▼
          │   [수락 확인 팝업] "이 제안을 수락하면 다른 대기 중인 제안은 자동 거절됩니다"
          │     │
          │     ├── [수락] → 채팅방 자동 생성 → 나머지 pending 제안 자동 거절
          │     │            → 제안자에게 수락 알림 + 거절된 제안자에게 자동 거절 알림
          │     └── [취소] → 돌아가기
          │
          └── [거절] 버튼
                │
                ▼
              [거절 사유 선택 바텀시트] (선택)
                ├── "다른 분과 진행합니다"
                ├── "조건이 맞지 않습니다"
                ├── "상태가 기대와 다릅니다"
                └── 직접 입력
                    │
                    ▼
                [거절 완료] → 제안자에게 거절 알림 (사유 포함)
```

**교환 제안 자동 처리**
```
[7일 미응답 자동 만료]
    │
    ├── 등록자가 제안에 7일간 미응답
    │     │
    │     ▼
    │   [제안 상태 → expired] → 제안자에게 "제안이 만료되었습니다" 알림
    │
    └── 동일 교환글에 1인 최대 3건 제안 가능 (다른 매물로)
          ├── 3건 초과 시도 → "이 교환글에는 이미 3건의 제안을 보냈습니다" 안내
          └── 취소/만료된 제안은 카운트에서 제외
```

---

## 5. 공구 플로우

### 5-1. 일반 공구 등록

```
[거래 유형: 공구 선택]
    │
    ▼
[공구 유형 선택] ── 일반 공구 / 세트 공구
    │
    │ (일반 공구)
    ▼
[일반 공구 등록 폼]
    ├── 상품명
    ├── 상품 사진 (최대 5장)
    ├── 1인당 가격 (배송비 포함 여부)
    ├── 목표 인원
    ├── 최소 인원 (선택)
    ├── 마감일
    ├── 배송 예정일
    ├── 상세 설명
    └── [등록하기]
        │
        ▼
    [진행자 자격 체크] ── 교환 5회+ & 긍정 90%+
        │
        ├── 자격 충족 → 등록 완료
        └── 미충족 → "공구 진행자 자격이 부족합니다" 안내
```

### 5-2. 세트 공구(캐릭터 선점) 등록

```
[공구 유형: 세트 공구 선택]
    │
    ▼
[세트 공구 등록 폼 — Step 1: 기본 정보]
    ├── 상품명 (예: "하이큐 아크릴 스탠드 풀세트")
    ├── 작품 선택 (DB에서)
    ├── 세트 구매 수량 (예: 2박스)
    ├── 상품 사진
    ├── 마감일 / 배송 예정일
    └── [다음]
        │
        ▼
[세트 공구 등록 폼 — Step 2: 캐릭터 설정]
    ├── 캐릭터 목록 자동 로드 (작품 DB 기반)
    ├── 캐릭터 추가/삭제 가능
    ├── 캐릭터별 선점 가능 수량 설정
    ├── 캐릭터당 가격 입력
    └── [다음]
        │
        ▼
[세트 공구 등록 폼 — Step 3: 확인]
    ├── 전체 정보 요약
    └── [등록하기]
        │
        ▼
    [등록 완료]
```

### 5-3. 공구 참여 (일반)

```
[공구 상세 페이지]
    │
    ▼
[정보 확인]
    ├── 상품명, 가격, 마감일
    ├── 현재 참여자 수 / 목표 인원
    └── 진행자 프로필 + 신뢰도
        │
        ▼
    [공구 참여하기 버튼]
        │
        ▼
    [본인인증 체크] ── 미인증 시 → 인증 플로우
        │
        ▼
    [참여 확인 팝업]
        │
        ├── 확인 → 참여 완료 + 채팅방 입장
        └── 취소 → 돌아가기
```

### 5-4. 세트 공구 참여 (캐릭터 선점)

```
[세트 공구 상세 페이지]
    │
    ▼
[정보 확인]
    ├── 상품명, 세트 가격, 인당 가격
    ├── 진행도 (선점 수 / 전체 수)
    └── 캐릭터별 선점 현황
        │
        ▼
    [캐릭터 선택]
        ├── 선점 완료된 캐릭터: 비활성 (이름 + 선점자 표시)
        └── 선점 가능한 캐릭터: [선점하기] 버튼
            │
            ├── 수량 선택 (남은 수량 범위 내)
            └── [선택 완료]
                │
                ▼
            [공구 참여하기 버튼]
                │
                ▼
            [본인인증 체크]
                │
                ▼
            [참여 확인 팝업]
                ├── 선택 캐릭터: OO
                ├── 결제 금액: O,OOO원
                └── [확인] → 선점 완료 + 실시간 수량 업데이트
```

### 5-5. 공구 진행 (진행자)

```
[마감일 도달 또는 목표 달성]
    │
    ▼
[진행자에게 알림]
    │
    ├── 목표 달성 시
    │     │
    │     ▼
    │   [진행자: 진행 확정 버튼 클릭]
    │     │
    │     ▼
    │   [공구 상태 → "진행 확정"]
    │     │
    │     ▼
    │   [진행자: 구매 진행]
    │     │
    │     ▼
    │   [진행자: 공지 업데이트] (구매 완료, 배송 시작 등)
    │     │
    │     ▼
    │   [진행자: 각 참여자에게 발송]
    │     │
    │     ▼
    │   [참여자: 수령 확인]
    │     │
    │     ▼
    │   [후기 작성]
    │
    └── 목표 미달 시 → "진행 결정 대기" 상태 진입
          │
          ├── [진행하기] → 최소 인원 이상이면 진행 확정
          └── [무산하기] → 공구 무산 처리 + 참여자 전원 알림
```

### 5-6. 공구 자동 무산 타이머 (진행자 잠수 방지)

```
[마감일 도달 — D+0]
    │
    ▼
["진행 결정 대기" 상태 진입]
    ├── 진행자에게 "진행/무산을 결정해주세요" 알림
    │
    ▼
[D+1 — 1차 리마인더]
    ├── 진행자에게 "진행/무산을 결정해주세요" 푸시 알림
    │
    ▼
[D+3 — 2차 경고]
    ├── 참여자 전원에게 "진행자 미응답" 알림
    ├── 진행자에게 "2일 내 미결정 시 자동 무산됩니다" 경고
    │
    ▼
[D+5 — 자동 무산]
    ├── 공구 상태 → "자동 무산"
    ├── 참여자 전원에게 "공구가 자동 무산되었습니다" 알림
    ├── 진행자에게 "잠수" 경고 1회 누적
    │
    └── [잠수 누적 제재]
          ├── 1회: 경고 알림
          ├── 2회: 신규 공구 등록 30일 정지
          └── 3회: 공구 진행자 자격 영구 박탈
```

---

## 6. 구함글 플로우

```
[거래 유형: 구함 선택]
    │
    ▼
[구함글 등록 폼]
    ├── 원하는 작품/캐릭터
    ├── 원하는 굿즈 타입
    ├── 상세 설명
    ├── 만료일 (기본 30일)
    └── [등록하기]
        │
        ▼
    [등록 완료]
        │
        └── (백그라운드) 관련 교환글 등록 시 → 구함글 작성자에게 알림
                         교환글 등록자에게도 → 관련 구함글 노출
```

### 6-1. 구함글 상세 열람

```
[구함글 카드 탭] (홈 > 구함글 섹션 / 검색 결과 / 목록 페이지)
    │
    ▼
[구함글 상세 페이지 — WANT01]
    │
    ├── [구함 정보]
    │     ├── 원하는 작품/캐릭터/굿즈타입
    │     ├── 상세 설명
    │     ├── 만료일 (D-N 표시)
    │     └── 등록일
    │
    ├── [작성자 프로필 카드]
    │     ├── 프로필 이미지 + 닉네임 + 신뢰등급
    │     ├── 교환 N회 / 긍정 N%
    │     └── [프로필 보기] → USER01-C
    │
    ├── [관련 교환글 추천] (같은 작품/캐릭터 교환글)
    │     └── 카드 탭 → EXCH02
    │
    ├── [채팅하기] → 채팅방 개설/입장 (로그인 필요)
    │
    ├── [공유] → 카카오톡/트위터/링크 복사
    │
    └── [더보기 (본인 글)]
          ├── 수정 → 구함글 등록 폼 (수정 모드)
          ├── 기간 연장 → 만료일 30일 추가
          └── 삭제 → 확인 팝업 → 삭제 완료
```

### 6-2. 구함 기반 매칭 알림

```
[교환글 등록 시 — 백그라운드]
    │
    ▼
[매칭 엔진: 작품+캐릭터 수준 비교] (Phase 1)
    │
    ├── 매칭되는 구함글 발견
    │     │
    │     ├── 구함글 작성자에게 → "찾으시는 굿즈와 유사한 교환글이 등록되었습니다!" 알림
    │     │     └── 알림 탭 → 교환글 상세(EXCH02)
    │     │
    │     └── 교환글 등록자에게 → "이 굿즈를 찾고 있는 분이 있습니다!" 알림
    │           └── 알림 탭 → 구함글 상세(WANT01)
    │
    └── 매칭 없음 → 알림 없음 (추후 등록 시 재매칭)
```

---

## 7. 이벤트 캘린더 플로우

### 7-1. 이벤트 열람

```
[이벤트 탭 (하단 네비)] 또는 [홈 > 다가오는 이벤트 > 더보기]
    │
    ▼
[이벤트 캘린더 페이지]
    │
    ├── [뷰 토글]
    │     ├── 캘린더 뷰 → 월별 달력에 이벤트 도트 표시
    │     ├── 리스트 뷰 → 날짜순 이벤트 목록
    │     └── 지도 뷰 → 카카오맵에 이벤트 핀 표시
    │
    ├── [필터]
    │     ├── 작품/아이돌/아티스트/배우
    │     ├── 캐릭터/멤버/캐스트/프로게이머
    │     ├── 이벤트 유형 (생일카페, 지하철 광고 등)
    │     ├── 지역
    │     └── 날짜 범위
    │
    ├── [날짜 선택] → 해당 날짜 이벤트 목록 표시
    │
    └── [이벤트 카드 탭] → 이벤트 상세 페이지
            │
            ▼
        [이벤트 상세]
            ├── 이벤트 정보 (이름, 기간, 장소, 설명)
            ├── 지도 (카카오맵)
            ├── 대표 이미지
            ├── 외부 링크
            ├── [찜] → D-3, D-1 리마인더 알림
            └── [공유] → 카카오톡, 트위터 공유
```

### 7-2. 이벤트 등록

```
[이벤트 등록 버튼] (캘린더 페이지 FAB)
    │
    ▼
[이벤트 등록 폼]
    ├── 이벤트 유형 선택
    ├── 작품/아이돌/아티스트/배우 검색 선택
    ├── 캐릭터/멤버/캐스트 선택
    ├── 이벤트명
    ├── 시작일 ~ 종료일
    ├── 운영 시간 (선택)
    ├── 장소 (주소 입력 + 지도 핀)
    ├── 상세 설명 (선택)
    ├── 대표 이미지 (선택)
    ├── 외부 링크 (선택)
    └── [등록하기]
        │
        ▼
    [등록 완료] → 즉시 캘린더에 노출
        │
        └── (관심 작품/캐릭터 등록한 유저에게 알림)
```

---

## 8. 생일/데뷔 배너 광고 플로우

```
[배너 신청 페이지] (메뉴 > 배너 신청)
    │
    ▼
[배너 신청 폼]
    ├── 대상 캐릭터/아이돌/아티스트/배우 선택
    ├── 배너 유형 (메인/사이드/팝업)
    ├── 게시 희망일 + 기간
    ├── 배너 이미지 업로드 (규격 안내 표시)
    ├── 저작권 가이드라인 동의 체크
    │     └── [가이드라인 보기] → 허용/비허용 목록 팝업
    └── [신청하기]
        │
        ▼
    [결제] (Phase 1.5~, 초기 무료)
        │
        ▼
    [관리자 검수 대기] (24시간 내)
        │
        ├── 승인 → "배너가 승인되었습니다!" 알림 → 예약 게시
        ├── 수정 요청 → "수정이 필요합니다" 알림 + 수정 사항 안내 → 재업로드
        └── 반려 → "게시가 반려되었습니다" 알림 + 사유 안내 → 환불 처리
```

---

## 9. 채팅 플로우

```
[채팅 탭 (하단 네비)]
    │
    ▼
[채팅 목록]
    ├── 진행 중인 교환/공구 채팅방 목록
    ├── 최신 메시지 미리보기
    ├── 안 읽은 메시지 수 뱃지
    └── [채팅방 탭]
            │
            ▼
        [채팅방]
            ├── 메시지 입력 + 전송
            ├── 이미지 전송 (S3 업로드)
            ├── [교환 확정] 버튼 (상단 바)
            │     └── 양쪽 클릭 시 → 교환 확정 상태로 변경
            ├── [운송장 입력] (교환 확정 후)
            │     └── 택배사 선택 + 운송장 번호
            ├── [교환 완료] 버튼
            │     └── 수취 확인
            └── [신고] 버튼
                  └── 신고 사유 선택 + 증거 첨부
```

---

## 10. 마이페이지 플로우

```
[마이 탭 (하단 네비)]
    │
    ▼
[마이페이지 메인] ─── USER01-A
    │
    ├── [프로필 카드 영역]
    │     ├── 72px 프로필 이미지 + 닉네임 + 인증 뱃지
    │     ├── 신뢰 지표 (교환 N회 · 긍정 N% · 가입일)
    │     ├── 최애 표시
    │     └── [프로필 편집] 버튼
    │           └── 프로필 편집 (/my/edit) ─── USER01-B
    │                 ├── 이미지 변경 (카메라/앨범/기본)
    │                 ├── 닉네임 변경 (30일 1회, debounce 500ms 중복확인)
    │                 ├── 최애 표시 설정 (20자)
    │                 ├── 관심 카테고리 변경 (바텀시트)
    │                 └── [저장] → PATCH API → 토스트 → 뒤로
    │
    ├── [퀵 카운트] ─ 내 교환글 N / 내 공구 N / 내 구함글 N
    │     └── 탭 → USER02 해당 탭으로 이동
    │
    ├── [내 활동 관리] ─── USER02
    │     ├── 내 교환글 (/my/exchanges) ─── USER02-A
    │     │     ├── 상태 필터: 전체/교환중/제안대기/완료
    │     │     ├── 카드 목록 (이미지+상품정보+상태뱃지)
    │     │     └── 롱프레스: 끌어올리기/수정/삭제
    │     ├── 내 공구 (/my/group-buys) ─── USER02-B
    │     │     ├── 서브탭: 진행중/참여한
    │     │     ├── 상태 필터: 7개 상태
    │     │     └── 카드 (프로그레스바+참여현황)
    │     ├── 내 구함글 (/my/wanted) ─── USER02-C
    │     │     ├── 카드 (만료일 D-N, 매칭 N건)
    │     │     └── 더보기: 수정/삭제/기간연장
    │     ├── 교환 내역 (/my/history) ─── USER02-D
    │     │     ├── 기간+유형 필터
    │     │     ├── 거래카드 (상대방+유형뱃지+완료일)
    │     │     └── 후기 작성하기 → 바텀시트 (긍정/부정+텍스트)
    │     ├── 받은 후기 (/my/reviews) ─── USER02-E
    │     │     ├── 후기 요약 (원형 게이지)
    │     │     ├── 후기 리스트 (긍정/부정+텍스트)
    │     │     └── 이의 제기 (72시간 이내)
    │     └── 찜 목록 (/my/wishlist) ─── USER02-F
    │           ├── 카테고리탭: 전체/교환글/공구/이벤트
    │           └── 찜 해제 (Optimistic UI + 되돌리기 토스트)
    │
    ├── [설정 아이콘] → 설정 (/my/settings) ─── USER03-A
    │     ├── 알림 설정 (7개 토글 + 방해금지 시간)
    │     │     └── 마스터 OFF 시 하위 비활성
    │     ├── 관심 카테고리 변경
    │     ├── 계정 관리
    │     │     ├── 비밀번호 변경 (바텀시트)
    │     │     ├── 소셜 연동 상태
    │     │     ├── 키워드 알림 설정 (/my/keywords) ─── USER03-B
    │     │     │     ├── 키워드 추가 (최대 20개, 2~30자)
    │     │     │     ├── 태그 칩 리스트 + X 삭제
    │     │     │     └── 칩 탭 → SRCH01 검색
    │     │     ├── 로그아웃 → 확인 팝업 → AUTH01
    │     │     └── 회원 탈퇴 ─── USER03-C
    │     │           ├── Step 1: 탈퇴 안내 (주의사항 4개)
    │     │           ├── Step 2: 탈퇴 사유 (라디오 5개)
    │     │           ├── Step 3: 본인 확인 (비밀번호/카카오)
    │     │           └── Step 4: 탈퇴 완료 → 3초 후 HOME01
    │     └── 정보 (약관/정책/라이선스/버전)
    │
    └── [고객센터]
          ├── FAQ
          ├── 1:1 문의
          └── 신고 내역 확인

[타인 프로필] ─── USER01-C (/user/[userId])
    │
    ├── 프로필 영역 (이미지+닉네임+인증뱃지+신뢰지표+가입일)
    ├── 등록 매물 탭 (교환글/공구/구함글) → 2열 그리드
    ├── 받은 후기
    └── [더보기] → 신고하기 바텀시트
```

---

## 11. 알림 플로우

```
[알림 아이콘 (`Bell`)]
    │
    ▼
[알림 목록 페이지]
    │
    ├── [교환 매칭] 알림 → 탭 → 매칭된 교환글 상세
    ├── [키워드 매칭] 알림 → 탭 → 해당 매물 상세
    ├── [공구 알림] → 탭 → 공구 상세 (목표 달성/마감 임박)
    ├── [채팅] 알림 → 탭 → 해당 채팅방
    ├── [이벤트] 알림 → 탭 → 이벤트 상세
    ├── [배너 검수 결과] → 탭 → 배너 신청 상세
    └── [시스템] 알림 → 공지사항 등
```

---

## 12. 신고 플로우

```
[신고 대상 (교환글/공구/구함글/이벤트/채팅/사용자 프로필)]
    │
    ▼
[신고 버튼 탭]
    │
    ▼
[신고 바텀시트]
    ├── 신고 사유 선택
    │     ├── 교환 미발송
    │     ├── 공구 먹튀
    │     ├── 허위 매물
    │     ├── 욕설/비하
    │     ├── 사진 도용
    │     └── 기타
    ├── 증거 첨부 (스크린샷, 사진)
    ├── 상세 설명 (텍스트)
    └── [신고 접수]
        │
        ▼
    [접수 완료 안내]
        │
        └── 48시간 내 1차 검토 → 결과 알림
```

### 12-1. 신고 결과 처리

```
[관리자 검토 완료] (48시간 내)
    │
    ▼
[처리 결과 알림 발송]
    │
    ├── [제재 조치] → 신고 대상자에게 알림
    │     ├── 경고 → "커뮤니티 가이드 위반으로 경고가 부여되었습니다" + 사유
    │     ├── 매물 삭제/숨김 → "등록하신 매물이 정책 위반으로 삭제되었습니다"
    │     ├── 일시 정지 (7일/30일) → "계정이 N일간 이용 정지되었습니다" + 해제일
    │     ├── 공구 강제 종료 → 참여자 전원에게 "공구가 관리자에 의해 종료되었습니다" 알림
    │     └── 영구 정지 → "계정이 영구 정지되었습니다" + 동일 번호 재가입 차단
    │
    └── [반려 (정상)] → 신고자에게 "검토 결과, 위반 사항이 확인되지 않았습니다" 알림

[제재 단계 누적]
    ├── 교환 미발송: 경고 → 7일 정지 → 영구 정지
    ├── 공구 먹튀: 즉시 영구 정지 + 번호 차단
    ├── 허위 매물: 매물 삭제 + 경고
    ├── 욕설/비하: 경고 → 30일 정지
    └── 사진 도용: 매물 삭제 + 경고

[신고 내역 확인] (마이페이지 > 고객센터 > 신고 내역)
    ├── 내가 접수한 신고 목록
    ├── 각 신고 상태 (접수/검토중/완료/반려)
    └── 처리 결과 상세 확인
```

---

## 13. 후기 작성 & 이의제기 플로우

### 13-1. 후기 작성

```
[교환/공구 완료 확정]
    │
    ▼
[후기 작성 요청 알림] (완료 직후 + D+3 리마인더)
    │
    ├── 알림 탭 → 후기 작성 바텀시트
    └── 마이페이지 > 교환 내역 > [후기 작성하기]
          │
          ▼
      [후기 작성 바텀시트]
          ├── 거래 상대 정보 표시
          ├── 평가: [긍정] / [부정] 선택
          ├── 텍스트 후기 입력 (선택, 최대 200자)
          └── [작성 완료]
                │
                ├── 확인 → 후기 등록 완료 + 상대방에게 알림
                │          (작성 후 수정/삭제 불가 안내)
                └── 7일 이내 미작성 → 작성 기한 만료 (추후 작성 불가)
```

### 13-2. 이의 제기

```
[받은 후기 목록] (마이페이지 > 받은 후기 — USER02-E)
    │
    ├── [부정 후기 확인]
    │     └── [이의 제기] 버튼 (후기 작성 후 72시간 이내만 노출)
    │           │
    │           ▼
    │       [이의 제기 바텀시트]
    │           ├── 이의 사유 선택
    │           │     ├── 허위 내용
    │           │     ├── 거래와 무관한 내용
    │           │     ├── 욕설/비하
    │           │     ├── 광고성
    │           │     └── 직접 입력
    │           ├── 상세 설명 (필수, 최대 500자)
    │           └── [이의 제기 접수]
    │                 │
    │                 ▼
    │             [접수 완료] → "72시간 내 검토 후 결과를 알려드리겠습니다"
    │
    └── [이의 제기 결과 수신]
          ├── 후기 삭제 → "이의가 인정되어 해당 후기가 삭제되었습니다"
          └── 이의 반려 → "검토 결과, 정당한 후기로 판단되었습니다"
```

---

## 14. 끌어올리기 플로우

```
[교환글/공구 상세 또는 내 활동 관리]
    │
    ▼
[끌어올리기 버튼/메뉴]
    │
    ├── 1일 1회 무료 → 즉시 끌어올리기 완료 + 목록 최상단 재노출
    │
    └── 추가 끌어올리기 (유료, Phase 1.5~)
          │
          ▼
      [결제 팝업]
          ├── 금액: 500~1,000원/회
          ├── 결제 수단 선택 (카카오페이/토스페이/카드)
          └── [결제하기]
                │
                ├── 성공 → 끌어올리기 완료 + 확인 토스트
                └── 실패 → 에러 안내 + 재시도
```

---

> **이 문서의 활용**: 각 플로우의 분기 조건(◆)과 화면 전환(→)을 기반으로 HTML 와이어프레임의 페이지 목록과 화면 간 연결 관계를 파악할 수 있습니다.

---

## 리비전 히스토리

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.0 | 2026-03-05 | 초안 작성 (전체 서비스 플로우, 회원가입, 교환, 공구, 구함, 이벤트, 배너, 채팅, 마이페이지, 알림, 신고) |
| v1.1 | 2026-03-05 | 3-1 검색 플로우 상세화 (필터 바텀시트, 자동완성 디바운스, 탭 바 등 SRCH01 기획서 반영) |
| v1.2 | 2026-03-06 | 2-0 로그인(AUTH01) 플로우 신규 추가, 2-1 회원가입(AUTH02) 4단계 스텝 상세화, 10. 마이페이지 플로우 USER01~03 반영 |
| v1.3 | 2026-03-06 | 4-4 EXCH05 교환 제안 관리 플로우 추가, 5-6 공구 자동 무산 타이머 상세화, 6-1/6-2 구함글 상세 열람+매칭 알림 추가, 12-1 신고 결과 처리 추가, 13 후기 작성 & 이의제기 플로우 추가, 14 끌어올리기 플로우 추가 |

---

*작성: 2026-03-06 | 버전: v1.3 | 기반: 애픽 월드 PRD v5.0*
